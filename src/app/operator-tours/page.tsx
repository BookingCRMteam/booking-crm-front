import { Container, Typography } from '@mui/material';

export default async function OperatorTours() {
  return (
    <Container maxWidth="lg">
      <Typography>Operators tours</Typography>
    </Container>
  );
}
