import { Container, Typography } from '@mui/material';

export default async function OperatorBookings() {
  return (
    <Container maxWidth="lg">
      <Typography>Operators bookings</Typography>
    </Container>
  );
}
